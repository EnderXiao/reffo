# 经历级 Writer 最小修复与 Case3 成品记录

日期：2026-09-08。最终状态：**Case3 成品已生成，最终代码校验 succeeded / deliver；不是生产上线验收通过。**

## 实施范围

1. 背景—行动关联在 renderingPlan 中保留，允许同一经历内分段说明；最终 Artifact 校验同步支持已选、无风险、同 scope 且有实际业务行动支撑的背景。跨 scope、风险材料、只有背景没有行动仍拒绝。
2. 摘要/技能中的普通能力归纳不必逐句重复团队词；数字成果、独立完成、主导等角色升级继续检查。将已有团队归因同时呈现在 Writer 事实文本中，减少复制时遗漏。
3. 数字跨行而单位不完整时，仅从写作视图移除残缺数值分句，保留同条完整成果。本次保留数字化率 100%，未猜补查看量，未提升下一段带口径限定的材料，原始证据未改。
4. 摘要补入已选、岗位相关的代表实践，不能只靠 JD 推出候选人事实。更新到 P06C_ENTRY r3，明确单段摘要和条目段落上限，移除普通资料来源标签，保留实质限定。
5. 单个辅助条目被模型拆成多段时无损合并；超过列表数量时只在同一经历中合并相邻段落，不删句、不改数字、不串项目。预算允许时项目和研究保留至少两段。仍遵守原 18 条列表和 2,700 字硬上限，未调大门禁预算。
6. 摘要出现无来源支持的正向“主导过”时，代码降为“参与过”并记录 warning，不把错误强归因当作通过；其他强动词仍交原校验处理，不做泛化语义修复。否定表达不进入该替换。

## 实测与复用边界

复用上一轮 14 个 P01、1 个 P02、1 个 P03 的原始真实响应。重放前核对请求全文，仅归一化运行 ID/随机 UUID；数据文件 SHA256 也一致。未手改缓存指纹、源数据或供应商响应。

本轮新增两次 DeepSeek V4 Flash Writer 非思考调用：

| 调用 | 输入 Token | 输出 Token | 结果 |
| --- | ---: | ---: | --- |
| r2 | 13,079 | 2,677 | 完整正文；摘要多段及归因问题，未交付 |
| r3 | 13,058 | 2,404 | 完整正文；后续代码衔接修复后以原始响应离线编译通过 |
| 合计 | 26,137 | 5,081 | 31,218 Token |

每次 Writer 输出上限仍是 4,320 Token，未调用 P08/P09/P12。最终编译与重验外呼为零。**不是修复后的全新冷启动 17 次调用，也不是 r3 供应商响应一返回便直接通过。**

r3 原始输出 13 个条目、26 段。最终保留全部内容，唯一词义调整为已记录的“主导过→参与过”；列表合并只影响布局。最终 7 个板块、2 个项目、12 条业务列表、18 条总列表，1,501 个汉字。

## 验证

- `bun test ./src`：986 pass / 0 fail，4,970 assertions，78 个文件。
- `bunx tsc --noEmit`：通过。
- `git diff --check`：通过。
- 最终原样重放：succeeded / deliver，事实安全、内容完整、可交付性代码检查通过，无 error。
- 句子保留核对：26 段均能在最终 Markdown 中找到；摘要按记录的降格替换后比较，其余原文未改。
- 已覆盖：同经历背景分段、背景选入渲染计划后关联不丢、风险背景仍拒绝、能力归纳与角色升级、跨行数值不猜补、辅助段落合并、条数压缩不删句、摘要降格、外来引用拒绝。

## 质量限制

成品项目有问题、方案和结果，资料自述口吻与错误查看量已消失；但方案判断和本人产物仍不够鲜明。摘要把 AI 辅导归纳到智能内容场景的表述仍偏宽，不能等同已有 AIGC 创意平台经历。部分工作列表较长，技能仍有泛化表达，平台日活不应被读成个人带来的增量。

仍有计划证据省略、技能软篇幅和布局规范化等 warning。程序核心要求覆盖 3/3 不等于满足完整 JD；本次没有新增真实招聘人员盲评、跨案例测试或前端端到端测试，不能据此批准生产上线。

## 本地交付与审计

目录：`.artifacts/v5-case3-entry-r2-20260908-gFOO6z/`，全部为本地私有产物，不提交。

- `r3/resume.md`：最终简历。
- `r3/final-result.json`：最终结构化结果及所有校验提示。
- `quality-review.md`：七板块质量评审。
- `r3/writer-request.json`、`r3/writer-response.json`：实际请求与原始响应。
- `r3/finalization.json`、`r3/verify-outcome.json`：真实响应复用与零外呼最终编译记录。
- 根目录与 r3 子目录的 `usage.json`：两次真实用量，原失败记录不覆盖。

未改环境配置、生产默认模式，未提交或推送。回退经历级实验可移除 `--entry-writer`；这不表示旧路径已经达到上线质量。
