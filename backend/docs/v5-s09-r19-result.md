# S09 r19 修复与真实重试结果

日期：2026-09-08。范围：修复匹配说明数字泄漏、协作对象误报，并执行一次有变化的 Writer 重试。未修改生产默认路径。

## 实现

- 写作意图升为 writing-intent-v2。用该意图实际引用且入选的源事实检查分析说明中的数字；若相似性/表达建议包含无引用依据的数字，移除这两段可复制说明，但保留关系、证据和可用的岗位差异。差异说明自身包含无依据数字时改用中性说明。
- 筛选覆盖既有阿拉伯数字表达和一组中文数量表达，是有界代码处理，不是全面语义认证；数字在来源出现也不等于同一职能年限或同一归因成立。
- 协作检查支持明确组织和“推动/促成 + 对象 + 具体行动”的有界表达，避免将“推动销售重新议价”误判为没有销售协作来源。业务增长指标或工序名不能仅凭同名词升级成协作部门。
- 实验 Writer r19 明确总职业年限不能改成细分职能年限，工序/业务指标不能自动改成部门。原默认 Writer r16 不变，正文契约与 4,320 输出上限不变。
- 新意图版本存入新计划；旧计划序列化继续保留 v1，已验证历史 Case3 请求未变。

## 验证

- 14 个新增回归用例；相关全套 858 项测试、4351 个断言通过。
- 后端 TypeScript、diff 空白检查通过。
- 预检确认 S09 实际准备的 Writer 输入中，不再通过 editorialIntents 携带无引用的 5 年经验说明。
- 一次 DeepSeek 非生产实际 Writer 调用成功，正常完成；结果 succeeded、deliverable=true、fallback=false，没有修复循环。
- P01 两片历史响应输入和当前提示词一致，并经当前代码重新校验；P02 是精确输入回放，P03 是历史已修复响应的当前代码重校验。本轮不是新版 P03 模型验收，也不是全链路从零测试。

私有产物：`.artifacts/v5-s09-r19-20260908-36VJFH/live/`；包含新生成 resume.md、result.json、写作计划、原始请求响应、质量抽查资料与预算。

## 原问题复测

| 问题 | 本轮实际结果 |
| --- | --- |
| 财务总经验被写成全部财务分析年限 | 摘要只描述制造业财务分析与预算管理背景，不再生成无当前句引用依据的 5 年 |
| 工序被写成同名部门 | 技能改为推动销售议价、工艺改善与停产评估，不再新增“工艺部门” |
| 团队金额归个人 | 约 480 万元仍带跨部门共同结果限定 |
| CPA 部分科目变为持证 | 明确保留专业阶段仅通过会计、财管两科 |
| 模型调用失败/无正文 | 正常返回，编译及代码交付通过，无 fallback |

## 质量结论与遗留项

本轮完成代码可交付和源材料对照的 agent 质量审阅，不是真实招聘人员盲评。538 个汉字、9 条业务要点、13 条列表、2 个项目。岗位匹配 42 分是候选人材料对目标岗位的距离，不是生成质量分或失败条件。

现有材料更能证明制造业预算、成本分析和财务建模；没有为用户增长财务岗位编造互联网行业、补贴 ROI 或增长实验经验。

仍有三项应记录而非忽略：

1. 第二段任职的“成本会计”标题在复用的 P01 timeline 中已经为 null，最终标题缺失；不是本轮 Writer 删除，但属于应该修复的身份展示遗漏。
2. 两条未引用计划证据中，一条为现金流模型和敏感性表的投资测算实践，与目标岗位资源配置/情景分析有潜在相关性，值得复核选材去向；另一条为财务技能概括，正文已有部分覆盖，不能把两个 ID 都算成两个能力缺失。
3. 教育正文重复学校、专业与日期，技能仍复述项目方法；属编辑问题，不据此认定全部生成失败。

因此结论为：**本轮针对性修复通过真实重试，S09 已完成一次可交付生成；整体生产验收仍未完成。** 不因岗位匹配偏低否定生成，也不以门禁通过掩盖身份显示和选材问题。

## 预算

- 本次输入 9,673、输出 1,211，共 10,884 Token，供应商延迟约 6.5 秒。
- 加上前一次失败的 S09 Writer，两次共 21,575 Token；失败调用仍完整计费。
- 全部累计：34 次、358,381 输入、95,734 输出。原总授权剩余 8 次、91,619 输入、4,266 输出 Token，无待结算调用。
- 该余额不足以预留下一次既有 4,320 输出额度的 Writer，更不足以刷新 S02/S05 的 P01。没有为了塞进余额降低输出上限，也未开启新一轮外呼。

## 下一阶段

S02/S05 的项目范围已变，仍须刷新受影响 P01 并做真实回归；S09 的旧任职标题需在提取/渲染层定位。可先做这些本地工作，待核算统一新增预算后再执行剩余外呼，不能从原“继续”指令推定无限额度。
