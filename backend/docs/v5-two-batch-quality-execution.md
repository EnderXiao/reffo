# V5 两批质量优化实施记录

日期：2026-09-07。增补既有 r2/r3 方案，不覆盖历史评审与上线门槛。

## 冻结基线

- 分支：`private/reffo-hsl`；HEAD：`a4526efb390b4633519dcacfb2ad5a4a83f85e5e`。
- 开始时工作区已有多项未提交变更，全部保留；HEAD 不是当前实现的完整指纹。
- 冻结时间：2026-09-07T11:19:47.969Z。
- `backend/src/v5` 全部 121 个文件按路径排序，以路径及原始字节依次计算 SHA256：`da49dfadb75af250c2a04f4fa6c099cd2beaccd4f154e69b6a9695a1408e61fa`。
- 基线结果：`.artifacts/v5-case3-reviewed-20260907-mYHXhC/result.json`，SHA256：`fb19ea616a238316cbd640242d6329786bcc21806808b208e3a628071c90d4a7`。
- 基线模型：`deepseek-v4-flash`，非思考模式；基线成品 succeeded / deliver，无回退，但招聘质量未通过上线验收。
- 保存的实际请求、原始响应、七板块评审不覆盖；真实简历仅保留在私有产物目录，不复制进本文件。

## 顺序与验收

1. A1：从有效实践关联构建有边界的编辑意图，不升级资格判断。
2. A2：同经历问题、约束、阶段作为辅助语境，保持原始证据与归属。
3. A3：独立可举证的岗位任务与紧凑写作指导。
4. 第一批本地验证、Case3 真实输出及质量比较。
5. B1：摘要、工作、项目分工；B2：实践型技能概括；B3：运行模式规则与职业化表达。
6. 综合回归与分层真实案例评估。样本不足不冒充上线验收。

不增加正常生成调用，不恢复 P08/P09 循环，不提高预算、不变更生产默认路径。引用可用、实际正文表达和招聘质量分别记录。

## 执行状态

- 基线 847 项测试通过。A1/A2/A3 实现后 866 项后端测试、类型检查通过；原资格合约、P01 与 Writer 输出结构不变。
- 离线 Case3：9 组去重编辑意图、3 条关联语境；AI 项目痛点可达 Writer，业务条目仍为 12。这里只证明信息可达，不证明成品表达质量。
- 第一次外呼连接失败，未取得供应商用量；预算保守结算不是实际账单。
- 网络权限调整后 P02 r4 实际返回正文 21,367 字符、推理 0 字符，7,200 输出 Token 截断；输入 7,563、总计 14,763 Token。尚未进入 P03/Writer。
- P02 r5 增加紧凑单行 JSON、去重复解释和直接关系引用指导，保持 7,200 输出上限；继续测试前单独记录新版本，不重用旧 P02 作为新实测。
- P02 r5 完整返回；P03 存在一条无效的可选主线引用，触发 P03R；后续需将这类可丢弃编辑建议局部降级，保持核心 link 非法引用仍阻断。
- Writer r10 首次输出被团队表达词法漏识别、摘要“负责→主导”升级阻断。补齐“跨部门交付”协作表达识别，仍保留所有权升级拒绝；r11 明确摘要不得升级动词。
- 第一批成品：`.artifacts/v5-case3-batch-a-writer-20260907-565l9o/`。实际 Writer r11 调用 succeeded / deliver、无回退；上游输入一致性验证后回放本轮真实响应。867 项本地测试、类型检查通过。
- 七板块比较已完成：语境与 AI 实践可见性改善，但重复、技能、教育展示尚不达标；匹配分变化不作为写作质量增益。开始第二批。

## 第二批实现与验证

- B1：按核心岗位任务提供摘要代表性事实建议；首条职责使用工作概览角色，项目侧重问题、动作及交付。只有明确来源别名可参与工作/项目的重复识别，不以公司名称相似建立项目归属。
- B2：启用服务端 `practice-skills-v1` 窄通道，从已选 action/responsibility/deliverable 及明确技能概括方法；技能可复用正文引用，但不增加正文业绩计数或事实召回。仍检查工具、数字、参与程度、阶段和具体协作部门。旧未启用策略的路径不自动获得例外。
- B3：受控 Writer 使用独立 `core-writing.md`，不再接收旧逐字摘录规则；允许按主题重组，而不是强制搬运源文首句。增加产品、财务、研究的正向示例，明确非量化交付和概念阶段也有价值。
- 职业化展示：去掉明确的“个人简历记录”等出处标签但保留实际数字与不确定性；时间线和教育共用格式化，时间线独立段落；同一已知结束年的区间与毕业日期合并展示，不推算缺失日期。
- 教育选材：定向路径优先明确学位，避免只挑选与标题重复的学校/专业；不从院校名称推测学历，不把所有成绩和奖项强制搬入成品。
- 可选 P03 narrative 引用无效时局部舍弃并警告；核心匹配 link 引用错误仍失败。真实回放证明本次不再需要 P03R；不意味着所有 P03 错误均无需修复。
- 回归：889 项后端测试、4,403 项断言、67 个文件通过；`bunx --no-install tsc --noEmit` 与 `git diff --check` 通过。合成用例覆盖销售、财务、客服、研究、应届、管理及英语表达；不是实际模型的跨行业质量验收。
- 第二批首次实际 Writer r12 返回完整正文，因“研发协同”反向表达未识别被代码误报。增加对称识别和反例测试后，对完全相同请求/响应零外呼回放得到 succeeded / deliver，无回退；记录于 `.artifacts/v5-case3-batch-b-verified-20260907-6DIOQD/`。
- 教育修复后的第二次 Writer 返回完整正文，但技能列举了引用证据未支持的协作部门；保留失败，未放松事实检查。r13 进一步要求摘要/技能的具体实践逐项引用，并避免堆砌部门名单。
- 一次因测试文件变化触发本地实现指纹不一致，发生在外呼前，新增 API 调用为零；重新冻结清单后继续，未覆盖旧记录。
- 最终 r13 实际 Writer 请求成功：输入 12,163、输出 1,718，共 13,881 Token。随后仅修正时间线段落间距，以完全一致的输入/响应零外呼重放得到 succeeded / deliver、无回退。最终产物及七板块报告：`.artifacts/v5-two-batch-case3-reviewed-20260907-7hnKJX/`。
- 第二批三次实际 Writer 请求合计 41,547 Token，包含失败稿；没有以成功一次的用量掩盖全部调试成本。未调用 P09/P12。
- Case3 成品已验证方法技能、问题背景、学位和日期展示改善；仍存在摘要/工作重复、摘要 AI 表述引用不完整、部分任职组织未展示等不足。代码引用合法不代表语义完整，本次评审没有将其判为全部质量通过。

### 验收范围

本轮内部非生产 `writer_v1 + job-targeted-v1` 路径；没有切换生产默认、部署、修改前端调用方式或执行 P09/P12。Case3 采用原始 P01 响应重新验证，P02/P03 只有提示词与输入一致才回放，Writer 使用真实新调用。不是所有阶段全部冷启动重新调用。

第一、二批实现完成不等于通用上线门槛达成。100 份真实黄金案例、30 份对抗输入、20 个同人多岗组及真实招聘人员评审仍按原方案执行；现有部分案例和 AI 辅助审阅不能替代这些资源。
