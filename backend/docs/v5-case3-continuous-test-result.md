# Case3 连续修复与真实测试：生成及质量评估已完成

日期：2026-09-07。用户授权在 Case3 范围内连续修复和非生产测试，不再逐轮确认，完成条件为成功生成简历并完成质量评估。

## 最终结果

- 最终状态 `succeeded`，交付判定 `deliver`；`usedSafeFallback=false`、`usedAnyFallback=false`。
- 最终稿已完成七个板块的招聘视角评估，**尚不认定达到产品上线质量**。现有客观 AIGC 岗位证据缺口与生成表达/结构问题分开记录，不要求候选人与目标岗位达到满分匹配。
- 采用真实源简历、真实 JD、DeepSeek Flash 非生产 API。为避免重复消耗，复用此前真实输出并按当前代码重新验证：没有修改原始响应、源简历或 JD，没有修改旧缓存指纹。
- 最终资料在 `.artifacts/v5-case3-reviewed-20260907-mYHXhC/`：`resume.md`、`quality-review.md`、`result.json`、`outcome.json`、`replayed-analysis.json`、`evidence-flow.json`。真实内容只保存在私有本地产物，不纳入提交。

## 修复内容

1. `chunked-resume-extraction.ts`：明确服务端 scope assignment 的负面边界。无 assignment 的概述材料不能借用业务 scope，原文、数字、归因和风险保留在 unscoped_source；业务标签降为 other，不删除材料、不补归属。显式空计划不能被模型虚构经历替换；未携带服务端计划的旧调用仍保持原行为。
2. `source-display.ts`、`evidence-routing.ts`、`writing/compiler.ts`：识别缺少终点的数值比较，避免「从百分比提升至」残句被当作完整成果选材与交付。不猜补数字，不将其他完整业务动作一起删除。已通过源续接证明合成的完整表达仍正常使用。
3. `writing/facts.ts`：团队贡献边界同时读取可信证据 riskFlags 和源文措辞，不再只从源句关键词推导；Writer 输入和输出检查保持一致，不降低归因要求。
4. 本轮非生产运行改用 Flash 非思考模式：P02 enabled/low 曾消耗 7,199 输出 Token 且全部为思考、正文为空。没有提高阶段额度；后续 disabled 正常返回。没有修改 `.env` 或生产默认模式。

## 真实调用与累计成本

统计范围为 r19 完整测试开始至本次最终稿，不含更早的 r17/r18 历史实验；来源是各运行的 usage.jsonl，不是估算账单。

| 阶段/尝试 | 新实际调用 | 输入 Token | 输出 Token | 结果 |
| --- | ---: | ---: | ---: | --- |
| r19 冷抽取及既定修复 | 15 | 97,304 | 42,498 | 第 1 片归属失败，已定位并修复 |
| 后续 P02 思考模式 | 1 | 7,436 | 7,199 | 全思考、无正文、截断 |
| 非思考 P02/P03/Writer | 3 | 45,289 | 12,419 | 首次生成成功；评审发现残缺指标 |
| 修复残缺指标后的 Writer | 1 | 8,753 | 1,423 | 摘要缺少团队归因；发现输入边界漏传 |
| 最终 Writer | 1 | 8,768 | 1,371 | 成功生成，通过代码交付检查 |
| **累计** | **21** | **167,550** | **64,910** | **总 Token 232,460** |

本轮连续修复新增 6 次调用、92,658 Token。一次私有回放脚本分隔符转义错误在外呼前失败，调用为 0，修正后继续，没有将其算成供应商失败。没有调用 P08/P09/P12。思考 Token 包含在供应商输出总量内，不重复加总或从实际消耗中扣除。

每个新外呼阶段均使用独立输出目录、独占启动标记、SDK maxRetries=0、仅官方 DeepSeek endpoint、有界组件与预算控制。最终 Writer 最多 1 次、4,320 输出上限，未消耗满额。

## 复用与真实性

P01 复用的不是被人工改写的缓存：对 14 次 r19 原始响应逐一验证模型、模式、finish_reason、源块、偏移、scope assignment、promptVersion；通过实际 stage adapter、当前归一化、分片领域校验及全量合并校验后，建立隔离内存缓存。源续接证明由当前代码从同一原文重建，旧缓存文件和指纹不改写。

P02/P03 回放要求原始系统提示词与用户输入结构匹配，只排除请求局部 runId/documentId；对完整正文重新做实际 Schema/领域处理。记录来源响应摘要及 inputVerified。最终 Writer 是新的 API 结果。整个结果由 `V5ResumeOptimizationWorkflow.run` 产生，不手改成品来伪装成功。

这证明了基于真实已验证上游结果的恢复链路，不等于新模型每次冷启动均会成功；反复扩大样本、上线或跨环境迁移仍需独立验收。

## 验证与质量边界

- 后端正确目录运行 `bun test src`：**847 通过、0 失败，62 文件、4,313 条断言**。
- `bunx --no-install tsc --noEmit`：通过；`git diff --check`：通过。
- 新回归覆盖无归属概述冒用服务端 scope、显式空计划、原数据不变、重复归一化、残缺数字比较、正常完整作文、元数据团队限定。
- 曾误在仓库根目录运行 Bun，混扫前端 Jest 测试产生失败；未据此修改前端或宣称前端测试通过。以上后端结果来自 backend 目录重跑。
- 最终引用审计无非法引用；完整简历质量评估不是模型自评分，不是正式招聘人员签认。
- 最终正文仍存在摘要偏泛、AI 项目展开不足、技能过于概括、时间线/教育展示不统一等问题。目标岗位的短视频内容生态、具体生成工具与模型评估经历缺乏充分源证据，不能通过写作虚构补齐。

**本次生成与评估任务完成，不等于上线门槛完成。** 下一阶段应围绕 JD 重点的选材篇幅、完整事实单元、基础信息渲染与独立质量评审优化，而不是以反复重跑换一份偶然高分结果。
