# P01 r17：分阶段思考配置与材料保留修正

日期：2026-09-07。分支 `private/reffo-hsl`。代码实现阶段真实外呼 **0**；随后经授权的代表片实验实发 **2** 次，因质量问题停止。保留工作区已有修改，不提交、不切换部署配置。

## 最新真实验证：未通过代表片验收

用户授权第 1、3、10、11 片后，已使用实际 Provider 和阶段解析代码串行测试第 1、3 片，均有完整正文并通过代码校验。第 1 片原始 23/23 块保留，0 未映射、0 排除、0 冲突；第 3 片原始 24/24 块保留，但模型将一段注明晋升已获批准的职责块标为 source_supported + future_or_planned，代码按已有风险规则将整段设为 excluded。重要职责材料因此失去可用性，达到约定停止条件；第 10、11 片未调用，无自动修复/重试。

实际输入 12,034、输出 6,648，共 18,682 Token，2 次均 stop；没有思考正文。已确认空正文未在这两片重现，但不能宣称 r17 质量通过。下一步要解决风险标记的原文依据与作用范围，不能直接清除全部风险或针对该句特判。本次未修改被测运行时代码或提示词，未重跑全案。私有报告：`.artifacts/v5-case3-p01-r17-live-20260907-LNgd68/REPORT.md`。

## 已完成

1. **独立配置**：新增 `DEEPSEEK_P01_THINKING_MODE=inherit|enabled|disabled`，默认 inherit。Provider 根据可信编译 metadata 识别 P01/P01R，兼容没有 manifest 的精确旧版本标签，不读取简历中的阶段名称。其余阶段继续使用全局模式和强度。禁用模式不发送 reasoning_effort，P01 空正文不会触发思考模式内部重试；原有阶段输出额度不变。
2. **指纹与诊断一致**：非生产 runner 的总配置记录全局与抽取策略；抽取缓存只绑定实际生效的抽取策略。P01 模式变化会改变抽取指纹，P01 disabled 时仅调下游思考强度不会使它失效。逐次私有请求记录采用该阶段的实际策略，Harness 请求/响应事件来自同一 Provider 解析。
3. **职责消歧**：新 `core-extraction.md` 仅用于 P01/P01R，明确 canonical source → 待校验候选，不要求抽取之前已有通过校验的 EvidenceAtom。其他阶段原 `core.md` 未修改。新基础提示词和注册逻辑进入抽取实现指纹。
4. **P01/P01R r17**：保留 9 个根字段及现有 transport/materializer。缺上下文、自述、缺外部佐证、前后值、子集/总量或数值断行不能自动等同实际冲突；保留原文、归属和限定，不自动补归属/因果。真实冲突、明确需确认和注入仍受安全规则约束。分类采用明确默认，避免反复权衡。r17 是当前分支上的新修订，不是假装恢复未验证的历史 r16。
5. **材料保留观察**：现有 `extraction.validation.observed` 的 domain 记录增加可选 `retention`，P01 与 P01R 都接入。分开统计原始事实块、未映射、漏块、代码补记、最终排除/限定、业务分类、无效冲突引用；只保存固定键和有界整数，不保存原文、理由或个人信息。老缓存和 Schema 失败不补造计数。

## 不做的事情

- 不把 source_qualified 或不确定信息自动提升为已证实成果，不恢复 P08/P09 循环，不新增 Agent 或提高 Token 额度。
- 不把账本覆盖、非 excluded 事实数或业务标签数量叫作“可用业务证据召回率”。真正可路由、已选材、进入正文还须使用证据去向报告及人工评审。
- `qualityAssessment` 暂保留 9 字段兼容契约，提示词仅允许本片评价；没有宣称已经迁移整份质量评分。若移除该字段，必须另行同步公共消费者和合并契约，不能制造假分数。
- 默认配置仍为 inherit，`.env.nonprod`/生产环境未改。下面 disabled 为后续非生产测试显式参数，不代表部署已切换。

## 验证结果

- `bun test src`：823 项通过，0 失败。
- `bunx --no-install tsc --noEmit`：通过，包含 runner。
- `git diff --check`：通过。
- Provider mock 测试覆盖真实编译的 P01/P01R/P02/P03/P06C metadata、逐阶段请求及事件、单次空响应不发生思考重试、继承默认、模式合法性和模型约束。
- 脱敏回归覆盖自述、前后值、子集/总量、数字断行在代码路径保留原文与 source_qualified，不引入冲突/时间线；实际风险不能变成 supported；观察字段的隐私投影、旧事件兼容与 P01/P01R 工作流接入。
- 三次零外呼预检验证：P01 disabled + 下游 low；P01 inherit + 下游 low；P01 disabled + 下游 high。均未初始化 Provider、外呼 0。前两者抽取指纹不同；第一、第三者抽取指纹相同但整次运行指纹不同。

Case3 新配置整案预检：14 片、冷缓存、0 个已验证 r17 片；调用上界 27/28，输出预算包络 337,756/350,000，输入预算 450,000，墙钟 15 分钟，blockingReasons 为空。这只证明预算与配置可以启动，不证明耗时、模型输出或最终质量会通过。

私有预检：`.artifacts/v5-p01-r17-preflight-20260907-ciFlfq/`。

- 实现摘要：`31537323e3b4180206bd09b2b9c129534f1a00402553c161fa8d0998cadeebea`
- 抽取实现摘要：`9480642942473a087d62b5d6d68682454308af2bad40508eb66a18aa06dbb481`
- 抽取配置摘要：`0d64d62bac51a94cce05f73cc9cfaa85b7a8eff4b7c01bda6ee94ff481f53e9b`

## 代表片测试计划（授权前记录，结果见上）

先验证新提示词真实行为，不直接整案重跑。已用当前编译器离线编译以下候选，尚未编写/运行新的多片真实执行器：

| Case3 片号 | 覆盖重点 | 目标块 | 单次输出上限 |
| --- | --- | ---: | ---: |
| 1 | 复查本次发现的自述/指标保留及混合身份分类 | 23 | 14,400 |
| 3 | 续片与只读锚点，同一经历归属 | 24 | 14,400 |
| 10 | 多 scope 材料和相对较长文本 | 24 | 15,398 |
| 11 | 多 scope 结构与材料保留 | 19 | 14,400 |

候选授权边界：仅 P01、Flash disabled，使用 r17 和现有编译器（temperature=0，区别于上一轮为了单变量实验而不传 temperature）；串行最多 4 次、总输出上限 58,598，输入预留上限 80,000（现有估算合计 27,805），墙钟 15 分钟。不发 JD、不进下游、不重试/P01R、不写抽取缓存。任一结构/证据错误或明显重要材料流失即停止后续片并分析，不循环抽样挑最好结果。

必须分别报告原始覆盖、服务端补记、限定/冲突/排除、有效材料保留和代码校验；即使 4 片通过也不等于 14 片合并或成品招聘质量通过。历史 r14 非思考首片成功只支持模式选择，不是 r17 实测。

## 后续与回滚边界

代表片通过后再按授权跑完整 Case3 及成品质量评估。其后仍须按最终计划拓展不同候选人和岗位，承认真实差距，不针对 Case3 优化到满分。

配置回退只需将 P01 模式恢复 inherit，不影响其他阶段。提示词回退需将 P01/P01R、抽取基础提示词、注册和指纹作为一组恢复；不能只换文本继续复用新缓存。不要使用破坏性 Git 操作回滚本工作区其他未提交修改。
