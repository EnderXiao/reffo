# V5 r18 结构编排优化执行记录

日期：2026-09-08。承接六组真实案例结构性试验；本轮不切换生产默认 Writer，不增加 Judge 或自动重写回环。

## 阶段一：先修复证据与代码校验

- P01 分片计划 v10：识别带独立项目标识及单月日期的标题，恢复独立经历边界；恢复时清除不属于该范围的组织、日期和标题，避免只是拆分正文、仍沿用前一项目身份。保持原有分片合并验证。
- 摘要不再因出现任何数字而一律失败；仍验证数字、单位、范围与归因是否有来源支持。
- 对源材料明确的学位及毕业/在读状态建立只读身份信号。冲突或未明确的状态保留未知，不根据日期推断；生成内容显式改变已知状态时报告错误。
- 对明确保留“平台整体规模并非本人模块独立承载”边界的输出消除误报；删除边界或反向声称个人独立承载仍不放行。
- P03 无证据的 explicit_gap 转为 unknown，并清除未经证明的负面断言。无依据的正向匹配不通过此规则自动放行。

## 阶段二：拆清匹配与写作职责

- P03/P03R 的 job-targeted-v1 路径使用独立的 job-fit-map 契约提示词，区分已有任务实践与尚未证明的业务环境/成功条件；不输出旧版评分或完整简历。旧路径及 P02 保持不变。
- 实验 Writer 提升到 r18：摘要突出主要优势及可选互补背景，不堆积证据；工作、项目、技能分别承担贡献、实际项目阶段、工具与方法的表达职责。
- 摘要和方法技能增加软长度预算，超长只告警，不自动截断或追加模型调用。
- 已入选的源技能中，若包含 JD 明确要求的常见具名工具，代码最多选两条源事实，在一个已有技能位置直接保留原文及熟练程度；Writer 不再重写该位置，其他技能位置仍可表达实践方法。
- 保持最终简历框架和公共输出结构；只增加内部写作计划字段。工具保留不代表证明实际项目用过该工具。

## 阶段三：验证范围

### 本地验证

- 832 个相关单测通过、4268 个断言；包含 V5、DeepSeek provider、持久化订阅和认证路由相关测试。
- 后端 TypeScript 检查及 diff 空白检查通过。
- 真实失败材料离线复核：S02、S05 各自两个独立项目恢复；S02 历史“毕业生写成在读”输出可被识别；S05 保留原平台归因边界不再误报；S09 原先三个空证据缺陷转为未知，历史响应通过。
- 上述是使用新代码重新处理、校验历史响应的诊断，不是 S02/S05/S09 新 P01 或新 Writer 的真实生成验收。

### 有界真实联调

私有产物目录：`.artifacts/v5-pipeline-r18-20260908-sZU8or/`。

本轮只做 Case3 原岗位联调：重新校验 14 片历史 P01 的输入、范围、响应与新缓存指纹；P02 历史请求精确匹配后回放；P03 与 r18 Writer 实际调用 DeepSeek 非生产 API。P03R 仅在需要时允许一次。最多 3 次真实调用，不调用 P08/P09。

沿用原六组测试总授权预算 42 次、450,000 输入 Token、100,000 输出 Token。先扣除历史保守计费 30 次、299,337 输入、86,562 输出；本轮不能借新运行目录重置总预算。

实际结果：`succeeded`、`deliverable=true`、`fallback=false`，2 次真实调用，未使用 P03R。P03 输入 28,080 / 输出 5,145，Writer 输入 11,622 / 输出 1,794；合计 46,641 Token，约 40 秒。两次响应均正常结束，未返回思考正文；供应商未单列 reasoning_tokens，因此不额外推断其计费组成。

含历史保守用量，累计 32 次、339,039 输入、93,501 输出；剩余 10 次、110,961 输入、6,499 输出。预算日志无待结算调用。本轮结束，不在剩余预算里自动启动新的扩大试验。

### Case3 质量结论

七板块仍齐全，12 条业务要点、18 条列表、2 个项目；总计 1,117 个汉字。匹配分仍为 59，不能以写作补齐 AIGC 内容平台、模型评估等候选人证据缺口。

| 板块 | 相比 r17 的观察 | 结论 |
| --- | --- | --- |
| 职业摘要 | 正文 163 → 87 个汉字，减少堆砌；仍包含职责和流程清单 | 阅读负担降低，但未完全实现定位式概括 |
| 工作经历 | 补回工作台研究动作；首条重新合并成长清单；iOS 日活 200K 仍遗漏 | 有增有退，不能认定改善 |
| 项目经历 | 两个项目身份与问题—动作链保留；没有补造实际上线成效 | 基本持平，不能替代目标行业证据 |
| 研究经历 | 研究规模、方法、交付和未上线边界保留 | 基本持平 |
| 专业技能 | 120 → 89 个汉字，仍复述埋点、双周发布和 AI 四类能力 | 更短，但跨板块重复未解决 |
| 教育背景 | 学位、成绩、奖学金保留 | 持平 |
| 荣誉奖项 | 四项奖项等级保留 | 持平 |

摘要汉字数不含渲染器添加的“职业概述”标签；技能数字包含三条正文。Writer 总 Token 从 r17 的 14,017 降到 13,416（约 4.3%），比 r16 的 14,786 低约 9.3%；不能据此宣称整链路成本同比下降。

判定：**代码误报和结构边界修复有明确验证证据；真实联调成功；写作质量只有局部改善，不替换生产默认版本。** 源简历存在但未保留的价值证据、工作首条的职责堆砌及技能重复仍值得定位。下一轮应先审查选材优先级和跨板块信息分配，再决定是否修改提示词，而非继续追加泛化写作禁令或追求匹配满分。

## 边界与未完成验收

- 单月项目标题、具名工具与学籍判断均为有界规则，不宣称覆盖全部语言、OCR 或表述形式。
- 具名工具保留仅覆盖已经进入写作计划的源技能，不负责恢复上游尚未提取/入选的全部技能。
- P03 与 Writer 同时更新的联调不能单独归因 Writer 的质量变化；需要冻结上游的成对试验才能评估单个组件。
- 未增加全面事实字典压缩或重构 P02；不将局部契约拆分描述为全链路重构。
- 六组原试验、离线复核与单份联调不能替代扩大样本、跨岗位评测及真实招聘人员盲评，也不构成生产上线验收。
