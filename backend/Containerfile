# Reffo Backend - Containerfile (Podman/Docker compatible)

FROM docker.io/oven/bun:1

WORKDIR /app

# 复制依赖文件
COPY package.json bun.lockb* ./

# 安装依赖
RUN bun install --frozen-lockfile

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 启动应用
CMD ["bun", "run", "src/index.ts"]
