## 本次经验

- 使用 skill：`xhs-writer`
- 任务结果：成功生成并校验 7 张设备 mockup 卡片
- 异常：`/v1/models` 可列出 `gpt-image-2`，但首次 `images/edits` 返回账号组未启用图片生成；用户要求再次尝试后调用成功
- 传输异常：出现过一次 TLS EOF 和一次响应流提前结束，逐张重试后恢复
- 可复用教训：模型可见不等于当前账号组拥有调用权限；批量生成前应先用单张卡探测真实生图权限，并把权限错误与瞬时传输错误分开处理
- 本次只记录为单次服务商经验，未据此修改全局 skill 规则
