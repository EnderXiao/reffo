# Reffo Bug 记录

本文档用于集中记录产品验证和回归测试中发现的 bug、修复状态和复核入口。

| ID | 日期 | 状态 | 页面/模块 | 问题 | 修复摘要 | 复核建议 |
| --- | --- | --- | --- | --- | --- | --- |
| BUG-20260708-001 | 2026-07-08 | 已修复 | 首页 -> 新的申请 -> JD 填写页 | 点击“继续申请/新的申请”后，目标岗位描述会带入上一轮岗位信息，新申请不是空白状态。 | 首页确认创建时清空 `useJDStore`，避免创建页从全局 JD 草稿中读取上一轮岗位内容。 | 先完成一次生成，再回到首页点击新的申请；公司、岗位名称、目标岗位描述均应为空。 |
| BUG-20260708-002 | 2026-07-08 | 已修复 | 首页卡片 -> 最佳简历详情页 | 首页卡片展示的简历评级与点击进入详情后的评级不一致，例如首页为 C，详情页为 D。 | 首页历史卡片改用 `qualityScore` 作为评级分数，并抽出 `resolveResumeGrade` 供首页和详情页共用同一套评级阈值。 | 生成一条质量分低于匹配分的记录后，从首页卡片进入详情页，两个位置的评级字母应一致。 |
| BUG-20260708-003 | 2026-07-08 | 已修复 | 首页简历卡片 | 公司名称超长换行后，岗位/工作地内容会向下挤压并与生成日期重叠。 | 为长公司名卡片增加 `company-wrap` 排版状态，扩高玻璃信息区并预留日期安全区，让公司、岗位、工作地与背景板整体上移。 | 使用“大萝卜特殊种植有限公司”等长公司名生成卡片，确认工作地与生成日期不重叠。 |
| BUG-20260708-004 | 2026-07-08 | 已修复 | 首页优化策略 | 首页优化策略最后一句固定重复“弱化泛化职责描述，强化效率提升、跨团队协同和可量化结果。”，没有展示后端返回结果。 | 移除前端模板拼接，历史卡片优先展示后端 `optimization_suggestions`，其次回退到 `changes_summary` / `analysis.suggestions`。 | 生成多条不同 JD 的历史记录，确认首页策略文案随后端返回内容变化。 |
